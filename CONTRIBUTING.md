# Contributing

Keep results deterministic for identical inputs. Any contract change must
update its JSON Schema, examples, and compatibility notes. Native behavior
must be compared with the Python oracle before replacing it.

Run formatting, Clippy, and tests before submitting changes.

